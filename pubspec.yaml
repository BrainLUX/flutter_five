name: todo
description: "Simple todo app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.6
  hive_generator: ^2.0.0

flutter:
  generate: true
  uses-material-design: true
